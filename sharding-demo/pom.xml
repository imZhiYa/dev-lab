<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.5</version>
        <relativePath/>
    </parent>

    <groupId>com.zhiya</groupId>
    <artifactId>sharding-demo</artifactId>
    <version>1.0.0</version>
    <name>sharding-demo</name>
    <description>ShardingSphere 机制验证实验室：EX-01~07，与分库分表知识库系列（shard-00~05）配套</description>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.parameters>true</maven.compiler.parameters>
        <!-- 锁知识库实验基线：ShardingSphere-JDBC 5.4.1（路由/改写/归并行为按此版本实测） -->
        <shardingsphere.version>5.4.1</shardingsphere.version>
    </properties>

    <dependencies>
        <!-- ShardingSphere-JDBC 核心：数据源工厂 + 分片规则 -->
        <dependency>
            <groupId>org.apache.shardingsphere</groupId>
            <artifactId>shardingsphere-jdbc-core</artifactId>
            <version>${shardingsphere.version}</version>
        </dependency>
        <!-- 数据源连接池（ShardingSphere 不自带，实验用 HikariCP） -->
        <dependency>
            <groupId>com.zaxxer</groupId>
            <artifactId>HikariCP</artifactId>
        </dependency>
        <!-- MySQL 驱动（版本随 parent 管理） -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
        </dependency>
        <!-- ShardingSphere 5.4.1 依赖 javax.xml.bind（JDK 21 已移除），补 JAXB 运行时 -->
        <dependency>
            <groupId>javax.xml.bind</groupId>
            <artifactId>jaxb-api</artifactId>
            <version>2.3.1</version>
        </dependency>
        <dependency>
            <groupId>org.glassfish.jaxb</groupId>
            <artifactId>jaxb-runtime</artifactId>
            <version>2.3.9</version>
        </dependency>
        <!-- ShardingSphere 5.4.1 依赖 snakeyaml 1.x（Representer 无参构造），
         spring-boot-parent 3.3.5 管理的是 2.x（安全版），显式锁回 1.33 -->
        <dependency>
            <groupId>org.yaml</groupId>
            <artifactId>snakeyaml</artifactId>
            <version>1.33</version>
        </dependency>
        <!-- 关闭 SLF4J 警告噪音（实验输出保持纯净） -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-nop</artifactId>
            <version>2.0.13</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <release>21</release>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>